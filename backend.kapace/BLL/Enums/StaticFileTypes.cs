namespace backend.kapace.BLL.Enums;

/// <summary>
/// Represents a folders that must contain files.
/// Enum names must be equals with folder names.
/// </summary>
public enum StaticFileTypes {
    /// <summary>
    /// Contains images by each content ids as folders.
    /// </summary>
    Content = 1,
}